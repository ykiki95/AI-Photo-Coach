name: ai_photo_coach
description: AI Photo Coach - 실시간 사진 구도 가이드 앱
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.5.0

dependencies:
  flutter:
    sdk: flutter
  ultralytics_yolo: ^0.1.46

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
